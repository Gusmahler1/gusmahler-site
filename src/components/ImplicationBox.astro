---
interface Props {
  items?: string[];
}

const { items = [] } = Astro.props;
---

{items.length > 0 && (
  <div class="ed__imp">
    <div class="ed__imp-lbl">Industry Implication</div>
    <ul class="ed__imp-list">
      {items.map((item) => <li>{item}</li>)}
    </ul>
  </div>
)}
